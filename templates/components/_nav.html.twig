<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">

  {# premier bloc menu #}
    <a class="navbar-brand" href="{{ path('index') }}">Le petit Vietnamien</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <div class="col">
        <ul class="navbar-nav">

          <li class="nav-item">
              <a class="nav-link" aria-current="page" href="#">menu</a>
          </li

          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="#">commander</a>
          </li>

          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{ path('newsletters_home', {'slug' : 'newsletter'}) }}">recevoir la newsletters</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="#">contact</a>
          </li>
        </ul>
      </div>

        {# second bloc menu #}
        <ul class="navbar-nav">
          {% if is_granted('ROLE_MANAGER') %}
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('admin_index') }}">Administration</a>
            </li>
          {% endif %}

          {% if is_granted('ROLE_USER') %}
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('users') }}">Mon profil</a>
            </li>
          {% endif %}  


          {% if app.user %}
            {# <a class="btn btn-outline-secondary mr-1" href="{{ path('service_calendar') }}">Calendrier</a> #}
            <a class="btn btn-outline-secondary mr-1"
                href="{{ path('app_logout') }}">Se déconnecter</a>
          {% else %}
            <a class="btn btn-outline-secondary mr-1" href="{{ path('app_login') }}">Connexion</a>
            <a class="btn btn-outline-secondary mr-1" href="{{ path('app_register') }}">Inscription</a>
          {% endif %}

   
   
      </ul>
    </div>
  </div>
</nav>