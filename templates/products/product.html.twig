{% extends 'base.html.twig' %}

{% block title %}NOS PRODUITS{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-9 justify-content-center mx-auto">

            <h3 class="title-PageProduct">Nos produits</h3>

            <div class="row product-container">
                <a href="{{ path('products_index') }}"> <i class='bx bxs-arrow-to-left'></i> Retour</a>

                {% for product in category.products %}
                    <div class="col-md-4 mt-4" >
                        <div class="card m-3">
                            <div class="position-relative">
                                <a href="">
                                    <img src="/uploads/images/{{ product.image }}" alt="{{ product.name }}" width="100%" style="height:250px;" class="img-fluid img-thumbnai card-img-tol"></a>
                                <div class="card-img-overlay d-flex justify-content-end align-items-end">
                                    <button type="button" class="btn btn-light rounded-circle" data-toggle="button" aria-pressed="false" autocomplete="off"><i class="far fa-heart"></i></button>
                                </div>
                            </div>
                            <div class="card-body">
                                <h6 class="card-title">Nom du produit : {{ product.name }}</h6> 
                                <span class="d-block"><span class="d-block title-PageProduct">Prix : {{ product.price }} € </span></span>
                                <span class="d-block"><span class="d-block title-PageProduct">
                                    {% if  product.stock  == 1  %}
                                    <small class="text-success">Produit disponible</small>
                                    {% else %}
                                    <small class="text-danger">Produit non disponible</small>
                                    {% endif %}
                                    </span></span>
                                    {% if  product.stock  == 1  %}
                                        <a href="{{ path("cart_add", {'id': product.id} ) }}"><button class="btn btn-warning">Ajouter</button></a>
                                    {% else %}
                                        <button class="btn btn-warning text-decoration-line-through" disabled >Ajouter</button>
                                    {% endif %}
                            </div>
                        </div>
                    </div>

                {% endfor %}
            </div>


        </div>
    </div>
{% endblock %}