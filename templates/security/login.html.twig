{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<div class="tele-height">

<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class="col-md m-5 d-flex justify-content-center">                  
        <div class="card text-center" style="width: 20rem;">
            <h1 class="h3 m-3 font-weight-normal">Page de connexion</h1>
            <div class="card-body">
                <label for="inputEmail" class="m-2">Email</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                <label for="inputPassword" class="m-2">Password</label>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">   
        

                <div class="text-center mt-3"><button type="submit" class="btn btn-block btn-gold">Se connecter</button></div>

                <p class="mt-3"><a href="{{ path('app_forgot_password_request')}}">mot de passe oublié</a></p>
            </div>
        </div>
    </div>
</form>
</div>

{% endblock %}
