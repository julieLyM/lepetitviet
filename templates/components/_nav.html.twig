<nav class="navbar navbar-expand-lg navbar-light" style="background-color: white;" id="navId">
  <div class="container-fluid">

  {# premier bloc menu #}
    <a class="navbar-brand" href="{{ path('index') }}"><img src="{{ asset('uploads/logoV.png') }} " alt="" width="50%">
</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <div class="col">
        <ul class="navbar-nav">

         <li class="nav-item">
            <a class="nav-link " aria-current="page" href="{{ path('index') }}#why-us">Actualités</a>
          </li>

          <li class="nav-item">
              <a class="nav-link " aria-current="page" href="{{ path('index') }}#menu">La carte</a>
          </li

          <li class="nav-item">
            <a class="nav-link " aria-current="page" href="{{ path('products_index') }}">Commander</a>
          </li>

          <div class="dropdown">
              <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                Contact
              </a>
              <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                <li class="nav-item">
                   <a class="nav-link " aria-current="page" href="{{ path('newsletters_home', {'slug' : 'newsletter'}) }}">Newsletter</a>
                </li>

                <li class="nav-item">
                  <a class="nav-link " aria-current="page" href="{{ path('post_contact') }}">Nous contacter</a>
                </li>
              </ul>
          </div>
        </ul>
      </div>

        {# second bloc menu #}
        <div class="mobile-nav-connex">
        <ul class="navbar-nav ">
        {% if app.user %}
                  <div class="hello">
                    Bonjour {{ app.user.username }}
                  </div>
          {% endif %}
          {% if is_granted('ROLE_USER') %}
    
          <div class="dropdown ">
              <a class="btn btn-gold dropdown-toggle " href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                Mon compte
              </a>
              <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <li class="nav-item">
                          <a class="nav-link" aria-current="page" href="{{ path('users') }}">Mon profil</a>
                        </li>
                        <li class="nav-item">
                          <a class="nav-link" aria-current="page" href="{{ path('cart_index') }}">Mon panier</a>
                        </li>
              </ul>
          </div>
          {% endif %}



          {% if app.user %}
            <a class="btn btn-gold m-3"
                href="{{ path('app_logout') }}">Se déconnecter</a>
          {% else %}
            <a class="btn btn-gold" href="{{ path('app_login') }}">Connexion</a>
            <a class="btn btn-gold m-3" href="{{ path('app_register') }}">Inscription</a>
          {% endif %}

   
   
          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
              <a class="btn btn-info btn-admin" aria-current="page" href="{{ path('admin_index') }}">Administration</a>
            </li>
   
          {% endif %}  
   
      </ul>
      </div>


      
    </div>
  </div>
</nav>